"use strict";

/**
Define a class (empty for now) that represents a recipe. Then create a new variable called recipe and
assign it to a new instance of that class.
**/

class Recipe {
  constructor() {
    console.log("creating a recipe instance");
  }
}

let recipe1 = new Recipe();
