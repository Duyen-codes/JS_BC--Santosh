const clickBtn = document.querySelector("#my-button");
const initClickGreeting = () => {
  console.log("Welcome!");
};

clickBtn.addEventListener("click", initClickGreeting);

// Sample usage - do not modify
initClickGreeting();
