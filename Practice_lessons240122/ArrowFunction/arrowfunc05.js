"use strict";

/**
In this challenge, you're asked to write the triple function from scratch as an arrow function.
The function triple receives a parameter and returns the result of multiplying it by itself.
**/

const multi = (a, b) => {
  return a * b;
};

// (optional) Sample usage
console.log(multi(2, 4));
