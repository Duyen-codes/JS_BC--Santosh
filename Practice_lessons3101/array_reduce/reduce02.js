"use strict";

/**
Calculate the sum of the grades array and store it in a variable called sum.

/**
 *
 */
let grades = [10, 5, 15, 20].reduce((total, current) => total + current, 0);

console.log(grades); // output: 50
// calculate the sum of these grades
